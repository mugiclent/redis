FROM redis:7

# Redis plugins/modules would be added here if needed.
# The official redis:7 image includes everything required for standard use.
# Config is mounted via docker-compose — see config/redis.conf.
